package cecs.algorithmanalysis.test;
import cecs.algorithmanalysis.RandomNumberGenerator;
import cecs.algorithmanalysis.InsertionSort;

public class TestInsertionSort {
    public TestInsertionSort(int[] arr) {
        /*
        int itr = 0;
        InsertionSort is = new InsertionSort();
        System.out.println("\nInsertion Sort:");
        while(itr < arr.length) {
            RandomNumberGenerator randomNum = new RandomNumberGenerator();
            int[] dataArr = randomNum.RandomNumberGenerator(arr[itr]);

            System.out.print("\n" + arr[itr] + " records: \t");
            System.out.print("\t");
            is.sort(dataArr);
            is.getElapsedTime();
            is.sort(dataArr);
            System.out.print("\t");
            is.getElapsedTime();
            is.reverseSort(dataArr);
            is.sort(dataArr);
            System.out.print("\t");
            is.getElapsedTime();
            ++itr;
        }

    */
    }
}
